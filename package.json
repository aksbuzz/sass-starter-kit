{
  "name": "saas-starter-kit",
  "private": true,
  "version": "0.1.0",
  "description": "Multi-tenant SaaS starter kit — Postgres-native, Fastify, Next.js",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "db:migrate": "docker compose run --rm migrate",
    "db:rollback": "docker compose run --rm migrate --no-dump-schema down",
    "db:status": "docker compose run --rm migrate status",
    "db:new": "docker compose run --rm migrate new",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:reset": "docker compose down -v && docker compose up -d"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0",
    "prettier": "^3.2.0"
  }
}
